# def run_feature(node: Node, model_image) -> None:
# Start model in docker

# Loop through all games
# Calculate expected ts
# If actual ts < max(expected ts)
# Call to rabbit
# And mark these as queued

# Receive sucesses from rabbit until all have succeeded.

# Stop model in docker

# Run all the features in order.
